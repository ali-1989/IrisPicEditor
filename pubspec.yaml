name: iris_pic_editor
description: A image editor package.
version: 0.1.2
homepage: https://iris.ir

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

  # use this: https://pub.dev/packages/bitmap
  # use this: https://github.com/chooyan-eng/crop_your_image

dependencies:
  flutter:
    sdk: flutter

  image: ^4.3.0                  #      {crop, rotate}
  image_editor: ^1.6.0           # noWeb,Windows    {crop, rotate [native]}
  #image_editor_pro

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
